apply from: "${project.rootDir}/gradle-tasks-for-npm.gradle"

task run(type: Exec) {
    dependsOn ":deployment:generateAwsCredentialsJson"
    dependsOn tsc
    commandLine npmExecutable
    args "start"
}
